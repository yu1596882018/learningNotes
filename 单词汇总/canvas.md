```
threejs.org
canvas.getContext('2d')//webgl
.moveTo()
.lineTo()
.stroke()
.fill()
.closePath()
.lineWidth
.beginPath()
.lineCap
.lineJoin
.setLineDash()
.getLineDash()
.lineDashOffset
.strokeStyle
.fillStyle
.rect(x,y,w,h)
.strokeRect()
.fillRect()
.clearRect()
.arc()
.arcTo(x0,x1,y0,y1,radius)
angle
radian
Math.PI
Math.cos()
Math.sin()
.fillText()
.strokeText()
.textAlign
.textBaseLine
.font
.measureText(文本).width  //获取文字长度
.dradwImage()//参数(img,x,y)、(img,x,y,w,h)、(img,sx,sy,sw,sh,x,y,w,h)
.translate(x,y)
.rotate()//值为弧度值
.scale(x,y)默认1为正常倍数
new Image()
.save()
.restore()
```
