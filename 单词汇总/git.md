```
git init
ls
ls -a
git config --global(决定是否全局)  (user.name "") (user.email "")
git config --list
git add 
git add .
git commit -m "注释"
git commit -a -m ""
git status
.gitignore
git diff
git diff --cached
git log
git log --onelinne
git diff [版本号1] [版本号2] 文件名
git reset --hard Head~
git reset --hard [版本号]
git reflog
git branch
git checkout 
git merge 
git branch -d 
git push 
git remote add 
git push -u
ssh-keygen -t rsa
git clone 
git pull (先要init)
git branch gh-pages
https://  yu1596882018（账户名）  .github.io/yuboke(仓库名)/
```
